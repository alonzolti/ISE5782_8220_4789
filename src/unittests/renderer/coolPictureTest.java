package unittests.renderer;

import org.junit.jupiter.api.Test;

import renderer.*;
import scene.Scene;
import primitives.*;

public class coolPictureTest {
    Scene scene = new Scene("cool scene").setBackground(new Color(75, 127, 90));
    
}
